@model Answer.Models.MajorModels

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>MajorModels</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.MajorType)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.MajorType)
            @Html.ValidationMessageFor(model => model.MajorType)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
